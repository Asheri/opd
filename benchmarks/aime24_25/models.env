# 模型 ID 配置（run_benchmark.sh 会 source 本文件）
# 学生 Qwen 系 ID 已按标准填写；JustRL-1.5B 的 HF ID 我无法联网查证，请填一次即可三组生效。

# —— 教师【对】（Direct-OPD Δ_T = logπ_RL − logπ_ref）——
# 用户提供（并行下载完成）：
#   教师 RL  π_RL  = hbx/JustRL-DeepSeek-1.5B   ← 蒸馏用的 JustRL 教师（用户称 "JustRL-1.5B"）
#   教师参考 π_ref = deepseek-ai/DeepSeek-R1-Distill-Qwen-1.5B
# 服务器本地路径见 /root/autodl-tmp/models/{JustRL-DeepSeek-1.5B, DeepSeek-R1-Distill-Qwen-1.5B}
export TEACHER_RL_PATH="hbx/JustRL-DeepSeek-1.5B"
export TEACHER_REF_PATH="deepseek-ai/DeepSeek-R1-Distill-Qwen-1.5B"
# TEACHER_PATH（run_benchmark.sh 的 --model teacher 用）默认取 RL 教师
export TEACHER_PATH="$TEACHER_RL_PATH"

# —— 学生（三组）——
export STUDENT_COMBO1="Qwen/Qwen3-1.7B"
export STUDENT_COMBO2="Qwen/Qwen3-4B"
# "R1-Distill-7B" = 论文合集中的 JustRL-R1-7B（7.6B）
export STUDENT_COMBO3="BytedTsinghua-SIA/JustRL-R1-7B"

# —— AIME 数据集（HF）——
export AIME24="hf:Maxwell-Jia/AIME_2024"
export AIME25="hf:yentinglin/aime_2025"

# —— 评估设置 ——
export EVAL_N_SAMPLES=1        # 1=greedy 基线；Avg@N（如 32）更稳但更慢
export EVAL_TEMP=0.0