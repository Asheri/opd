# 模型 ID 配置（run_benchmark.sh 会 source 本文件）
# 学生 Qwen 系 ID 已按标准填写；JustRL-1.5B 的 HF ID 我无法联网查证，请填一次即可三组生效。

# —— 教师（三组共享）——
# TODO: 填 JustRL-1.5B 的准确 HuggingFace 模型 ID（如 org/JustRL-1.5B）。
# 确认方式：huggingface.co 搜 "JustRL" 或 modelscope 对应 repo。
export TEACHER_PATH="PLAZCEHOLDER_JUSTRL_1_5B"

# —— 学生（三组）——
export STUDENT_COMBO1="Qwen/Qwen3-1.7B"
export STUDENT_COMBO2="Qwen/Qwen3-4B"
export STUDENT_COMBO3="Qwen/Qwen3-R1-Distill-7B"   # 如为 DeepSeek 系 R1-Distill-Qwen-7B 请改

# —— AIME 数据集（HF）——
export AIME24="hf:Maxwell-Jia/AIME_2024"
export AIME25="hf:yentinglin/aime_2025"

# —— 评估设置 ——
export EVAL_N_SAMPLES=1        # 1=greedy 基线；Avg@N（如 32）更稳但更慢
export EVAL_TEMP=0.0